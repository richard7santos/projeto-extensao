import { HeaderContainer, Logo, Title } from "./Header.styles";

const Header = () => {
    return (
        <HeaderContainer>
            <Logo />
            <Title>Menu</Title>
        </HeaderContainer>
    );
}
export default Header;